# 開發工作流程

> **核心理念:** Sequential Thinking 先行 + BDD → Spec → TDD → Code
>
> **AI 自動遵循此流程，無需用戶指示。**

---

## 核心原則

1. **Sequential Thinking 先行** - 所有任務都先用結構化思考分析
2. **BDD 定義行為** - 用 Gherkin 語法描述預期行為
3. **Spec 產生規格** - 根據行為產生技術規格
4. **TDD 測試先行** - 先寫測試，再寫程式碼
5. **Code 實作** - 實作讓測試通過
6. **Review 審查** - 程式碼審查確保品質

---

## 工作流程三大支柱

| 工具 | 用途 | 適用場景 |
|-----|------|---------|
| **MCP 工具** | Sequential Thinking（所有任務第一步）、context7、tailwindcss-mcp | 結構化思考、程式碼生成 |
| **Spec Commands** | `/bdd`、`/backend-spec`、`/frontend-spec`、`/tdd`、`/impact-analysis`、`/components`、`/review-staged` | 需求定義、規格、測試、審查 |
| **Agents** | researcher、tester、coder、reviewer 等 | 程式碼實作、測試、審查 |

---

## Spec Commands 指令列表

| 指令 | 用途 | 階段 |
|-----|------|-----|
| `/bdd [描述]` | 定義行為需求（Gherkin 語法） | **1. 需求定義** |
| `/backend-spec [描述]` | 生成後端技術規格文件 | **2. 技術規格** |
| `/frontend-spec [描述]` | 生成前端技術規格文件 (Blade + Tailwind) | **2. 技術規格** |
| `/components` | 掃描並列出可用共用組件 | **2. 技術規格（UI）** |
| `/tdd [描述]` | 測試驅動開發（紅→綠→重構） | **3. 測試先行** |
| `/impact-analysis [目標]` | 分析變更影響範圍 | **修改前評估** |
| `/review-staged` | 審查 Git 暫存區變更 | **提交前審查** |

---

## 影響範圍分析

| 指令 | 用途 | 使用時機 |
|-----|------|---------|
| `/impact-analysis [目標]` | 分析程式碼變更影響範圍 | 修改前評估風險 |

**支援的分析目標：**
- 單一檔案：`/impact-analysis app/Services/ProductService.php`
- 多個檔案：`/impact-analysis ProductService.php ProductRepository.php`
- 類別名稱：`/impact-analysis ProductService`
- 方法：`/impact-analysis ProductService::createProduct`
- 功能描述：`/impact-analysis "修改產品價格計算邏輯"`

**分析內容：**
- 依賴關係圖（誰依賴這個、這個依賴誰）
- 受影響的檔案清單（直接/間接）
- 需要執行的測試
- 風險等級評估
- 建議執行順序

---

## Agents 完整列表

**如何調用 Agents:**
```bash
Task agent: [agent名稱]
Prompt: "[具體任務描述]"
```

| 分類 | Agent | 用途 |
|-----|-------|------|
| **核心開發** | `researcher` | 分析程式碼結構、研究依賴關係 |
| | `coder` | 實作程式碼、遵循規範 |
| | `tester` | 編寫測試、驗證品質（支援 TDD） |
| | `reviewer` | 程式碼審查、檢查規範、**高內聚低耦合檢查** |
| **模板生成** | `base-template-generator` | 建立基礎模板和樣板程式碼 |
| **UI/UX** | `ui-designer` | UI 設計、設計系統 |
| | `component-architect` | 元件架構設計 |
| **架構** | `system-architect` | 系統架構設計、技術選型 |
| **協調** | `pr-manager` | Pull Request 管理 |

### Reviewer 必須檢查項目

**高內聚檢查：**
- [ ] 類別只負責單一職責？
- [ ] 方法只做一件事？
- [ ] 方法長度不超過 50 行？
- [ ] 類別名稱能準確描述其職責？

**低耦合檢查：**
- [ ] 使用依賴注入而非直接 `new`？
- [ ] 依賴介面而非具體類別？
- [ ] 無循環依賴？
- [ ] 遵循分層架構（Controller → Service → Repository）？

---

## MCP 工具

| 工具 | 用途 | 使用時機 |
|-----|------|---------|
| `context7` | 確保最新 Laravel/PHP 語法 | **所有程式碼生成**（Controller/Service/Repository/Model） |
| `tailwindcss-mcp-server` | Tailwind CSS 規範與元件生成 | **僅前端程式碼**（Blade/HTML/CSS） |
| `sequentialthinking` | 結構化思考與問題分解 | **複雜問題分析、架構設計、方案比較** |

**MCP 使用規則：**
- ✅ 寫 PHP 程式碼時：**必須**使用 `context7`
- ✅ 寫 Blade 模板時：**必須同時**使用 `context7` + `tailwindcss-mcp-server`
- ✅ 寫純 CSS/Tailwind 時：**必須**使用 `tailwindcss-mcp-server`
- ❌ 只寫文件、規格時：不需要使用 MCP 工具

---

## 完整開發流程

```
📝 階段 1: 規格定義
   └─ Sequential Thinking (需求分析)
   └─ /backend-spec [功能描述] (後端規格)
   └─ /frontend-spec [功能描述] (前端規格)

🔍 階段 2: 需求研究
   └─ Task agent: researcher (分析現有程式碼和需求)

🏗️ 階段 3: 技術規劃
   └─ Task agent: system-architect (系統架構設計) [可選]
   └─ Task agent: ui-designer (UI/UX 設計) [可選]
   └─ Task agent: component-architect (元件架構) [可選]

💻 階段 4: 開發實作（TDD 方式）
   4.1 建立基礎架構
       └─ Task agent: base-template-generator
       └─ 使用 MCP: context7 (生成 PHP 類別)

   4.2 測試先行（TDD）
       └─ Task agent: tester (先寫測試)
       └─ 使用 MCP: context7 (PHPUnit 測試)

   4.3 後端開發
       └─ Task agent: coder (實作功能讓測試通過)
       └─ 使用 MCP: context7 (Controller/Service/Repository/Model)

   4.4 前端開發
       🚨 強制步驟：
       └─ 第一步：使用 /components 查看可用共用組件
       └─ 第二步：確認使用哪些共用組件
       └─ 第三步：Task agent: coder (開發時優先使用共用組件)
       └─ 使用 MCP: context7 + tailwindcss-mcp-server (Blade 模板)

✅ 階段 5: 品質保證
   └─ Task agent: reviewer (程式碼審查)
       使用 MCP: context7 (審查 PHP/Blade)

🚀 階段 6: 發布管理
   └─ Task agent: pr-manager
```

---

## 常見場景流程（AI 自動執行）

### 場景 A: 新功能開發
```
0. Sequential Thinking - 分析需求
1. /bdd - 定義行為（Given/When/Then）
2. /backend-spec - 產生後端規格
3. /frontend-spec - 產生前端規格（如有 UI）
4. /tdd - 先寫測試（紅燈）
5. coder agent - 實作（綠燈→重構）
6. reviewer agent - 審查
7. /review-staged - 提交前審查
```

**範例提問：**
```
「幫我開發一個產品管理系統」
「新增一個客戶資料模組」
「實作訂單追蹤功能」
```

### 場景 B: Bug 修復
```
0. Sequential Thinking - 分析問題根因
1. /impact-analysis - 評估影響範圍
2. /tdd - 先寫失敗測試重現 bug
3. coder agent - 修復讓測試通過
4. reviewer agent - 審查
5. /review-staged - 提交前審查
```

**範例提問：**
```
「產品列表載入很慢，幫我修復」
「訂單金額計算錯誤」
「這個查詢有 N+1 問題」
```

### 場景 C: UI 開發
```
0. Sequential Thinking - 分析 UI 需求
1. /components - 列出可用共用組件
2. /bdd - 定義 UI 行為
3. /frontend-spec - 產生前端規格
4. /tdd - 先寫 Feature Test
5. coder agent - 實作 UI（使用共用組件）
6. reviewer agent - 審查
7. /review-staged - 提交前審查
```

**範例提問：**
```
「幫我做一個產品列表頁面」
「新增訂單編輯表單」
「設計一個儀表板畫面」
```

### 場景 D: 重構
```
0. Sequential Thinking - 分析重構策略
1. /impact-analysis - 評估影響範圍
2. /tdd - 先寫回歸測試
3. coder agent - 重構（保持測試通過）
4. reviewer agent - 審查
5. /review-staged - 提交前審查
```

**範例提問：**
```
「幫我重構 ProductService」
「把這個邏輯移到 Service 層」
「優化這段查詢效能」
```

### 場景 E: 修改共用組件
```
0. Sequential Thinking - 分析組件需求
1. /components - 掃描現有組件
2. /impact-analysis - 評估修改影響
3. /tdd - 撰寫組件測試
4. coder agent - 實作組件
5. reviewer agent - 審查
6. /review-staged - 提交前審查
```

**範例提問：**
```
「修改 action-buttons 組件」
「新增一個共用的 modal 組件」
「這個組件需要加新參數」
```

### 場景 F: 程式碼提交
```
1. /review-staged - 審查暫存區變更
2. 根據審查結果提交或提示修正
```

**範例提問：**
```
「幫我 commit」
「準備提交 PR」
「審查一下我的變更」
```

---

## 工作流程決策樹

```
開始新任務
    │
    ├─ 0. Sequential Thinking（所有任務第一步）
    │
    ├─ 是新功能嗎？
    │   └─ 是 → /bdd → /backend-spec → /frontend-spec → /tdd → coder → reviewer
    │
    ├─ 是 UI 開發嗎？
    │   └─ 是 → /components → /bdd → /frontend-spec → /tdd → coder → reviewer
    │
    ├─ 是 Bug 修復嗎？
    │   └─ 是 → /impact-analysis → /tdd → coder → reviewer
    │
    ├─ 是重構嗎？
    │   └─ 是 → /impact-analysis → /tdd（回歸測試） → coder → reviewer
    │
    ├─ 是架構設計嗎？
    │   └─ 是 → /backend-spec → reviewer
    │
    ├─ 是組件相關嗎？
    │   └─ 是 → /components → /impact-analysis → /tdd → coder → reviewer
    │
    ├─ 是提交程式碼嗎？
    │   └─ 是 → /review-staged → commit
    │
    └─ 是快速修改嗎？（用戶說「直接改」）
        └─ 是 → 直接編輯
```

### 何時使用 /impact-analysis

| 場景 | 是否需要 | 原因 |
|-----|---------|------|
| **新功能開發** | ❌ 不需要 | 新程式碼沒有既有依賴 |
| **Bug 修復** | ✅ 建議 | 了解修復可能影響的範圍 |
| **重構** | ✅ 必須 | 確保不破壞現有功能 |
| **修改 Model** | ✅ 必須 | Model 變更影響最大 |
| **修改 Service** | ✅ 建議 | 商業邏輯變更需評估 |
| **修改 Controller** | ⚠️ 視情況 | 簡單修改可跳過 |
| **修改共用組件** | ✅ 必須 | 影響所有使用處 |
| **修改 Blade 頁面** | ❌ 不需要 | 影響範圍明確 |
